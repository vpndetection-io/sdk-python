name: Integration

# Exercises the package as PUBLISHED on PyPI, against staging. CI covers this working
# tree; nothing there would notice a tag that was never pushed, an sdist that ships no
# `src`, or a release that never landed.
on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:
  schedule:
    # Off the hour on purpose: scheduled runs bunched at :00 are queued behind everyone
    # else's and can be dropped entirely under load.
    - cron: '23 6 * * *'

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      # No pip cache: the point is the download a stranger gets. The script skips with a
      # reason when nothing is published yet; a tier whose key is missing skips from
      # inside the suite.
      #
      # One org per plan tier, because the tier comes from the ORG rather than from the
      # key. A secret that does not exist interpolates to an empty string here, which the
      # suite treats as absent.
      - run: python3 scripts/run.py
        working-directory: integration
        env:
          VPNDETECTION_STAGING_KEY_FREE: ${{ secrets.VPNDETECTION_STAGING_KEY_FREE }}
          VPNDETECTION_STAGING_KEY_STARTER: ${{ secrets.VPNDETECTION_STAGING_KEY_STARTER }}
          VPNDETECTION_STAGING_KEY_SCALE: ${{ secrets.VPNDETECTION_STAGING_KEY_SCALE }}
          VPNDETECTION_STAGING_KEY_MAX: ${{ secrets.VPNDETECTION_STAGING_KEY_MAX }}
